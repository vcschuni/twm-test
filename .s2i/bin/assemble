#!/bin/bash
set -e

# Only run composer if it exists
if [ -f composer.json ]; then
    echo "Running composer install..."
    composer install --no-dev --optimize-autoloader
fi

# Do NOT manually mkdir or copy files; S2I handles that automatically
